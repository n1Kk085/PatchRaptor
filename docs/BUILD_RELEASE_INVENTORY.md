# Build Release Inventory - Source of Truth

This document represents the **AUTHORITATIVE** list of files included in the PatchRaptor release package.
It is derived directly from the build_release.bat script and verified against the current codebase state.

---

## 📦 COMPLETE RELEASE PACKAGE (`dist/` folder contents)

### 🔧 Core Executables (5 Files)

| File | Size Type | Purpose |
|------|-----------|---------|
| `Configurator.exe` | Self-extracting Python app | Generates config.json from user input during setup |
| `Instinct.exe` | Core bot executable | Headless Discord bot service - the actual running application |
| `PatchRaptor.exe` | GUI launcher executable | Controls Instinct.exe (Start/Stop), displays console output |
| `RaptorChat.exe` | Chat relay executable | Cross-cluster in-game chat bridge service |
| `WebPanel.exe` | Web dashboard executable | Flask-based web interface for server monitoring |

### 📁 Configuration Templates (2 Files)

| File | Purpose |
|------|---------|
| `config.example.json` | Main application configuration template (paths, tokens, servers) |
| `webpanel_config.example.json` | Web panel authentication template (username/password) |

### 📚 Documentation Files (4 Files)

| File | Purpose |
|------|---------|
| `commands.md` | Complete Discord command reference (30 commands documented) |
| `discord_setup.md` | Step-by-step Discord bot creation and configuration guide |
| `readme.md` | Main documentation / getting started guide |
| `features.md` | Feature list summary (server monitoring, controls, updates, etc.) |

### 🔌 Tunnel Scripts (3 Files)

| File | Purpose |
|------|---------|
| `setup_tunnel.bat` | Cloudflare Tunnel initial setup and binding configuration |
| `start_tunnel.bat` | Manual tunnel start command (alternative to `.webpanel on`) |
| `update_cloudflared.bat` | One-click Cloudflare Tunnel binary updater |

### 📋 Additional Files (1 File)

| File | Purpose |
|------|---------|
| `LICENSE` | Controlled Source License agreement text |

---

## 📊 TOTAL FILE COUNT

| Category | Count |
|----------|-------|
| Executables (.exe) | **5** |
| JSON Templates | **2** |
| Documentation (.md) | **4** |
| Batch Scripts (.bat) | **3** |
| License Files | **1** |
| **TOTAL FILES** | **15** |

---

## 📝 NOTES ON RELEASE PACKAGE CONTENTS

### ✅ INCLUDED BY DEFAULT
- All executables are self-contained with PyInstaller bundles
- Documentation is comprehensive for end users and administrators
- Configuration templates include inline comments
- Tunnel scripts handle Cloudflare integration automatically

### ❌ NOT INCLUDED (Intentionally Excluded)
- `requirements.txt` - Bundled into each .exe via PyInstaller
- `.venv/` directories - Python virtual environments not included
- `_pycache/` folders - Bytecode cache files excluded
- Test files (`tests/`) - Development testing infrastructure excluded
- Generated runtime files - These are created during operation:
  - `config.json` (generated by Configurator)
  - `webpanel_config.json` (user creates from template)
  - `version.txt`, `schedule.json`, `bans.json` (runtime data)
  - `logs/` directory (created at runtime)

### 🔄 CONDITIONAL FILES
Some files may only be copied to dist if they exist in the source:
- `readme.md` → Falls back to `release_readme.md` if main readme missing
- All documentation is optional but recommended for production releases

---

## 🔍 FILE VERIFICATION STATUS

All listed files have been verified against actual build_release.bat execution and confirmed to be present in released packages.

**Last Verified**: Source code state as of audit completion  
**Build Script Reference**: `build_tools/build_release.bat` lines 50-67 (file copy operations)

---

## 📝 VERSION HISTORY

This inventory document tracks what changes to the release package contents over time.

### Current Version: 1.0 (Accurate Source of Truth)
- Created based on comprehensive codebase audit  
- Reflects actual build_release.bat behavior
- Verified against current repository state

---

## 🚀 DEPLOYMENT NOTES

When deploying a release package:

1. **Extract to clean directory** - Overwriting runtime files with templates is safe
2. **Run Configurator.exe first** - Required to generate config.json
3. **Tunnel scripts are optional** - Only needed for remote web panel access
4. **Documentation can be removed** - For minimal server deployments if desired

For production servers, remove non-essential documentation files to reduce package size.
</END_FILE>